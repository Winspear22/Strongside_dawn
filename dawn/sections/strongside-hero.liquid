{% comment %}
  STRONGSIDE Product Hero - Exact React Replica
{% endcomment %}

{%- assign product = section.settings.product -%}

{%- comment -%} SMART AUTO-SELECT LOGIC: Find product with "Harness" in title if not selected {%- endcomment -%}
{%- if product == blank -%}
  {%- for p in collections.all.products -%}
    {%- assign title_down = p.title | downcase -%}
    {%- if title_down contains 'harness' or title_down contains 'harnais' -%}
      {%- assign product = p -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}
{%- endif -%}

{%- comment -%} Fallback to specific handle or first product if still not found {%- endcomment -%}
{%- if product == blank -%}
  {%- assign product = all_products['strongside-pro-boxing-harness'] -%}
  {%- if product == blank -%}
     {%- assign product = collections['all'].products.first -%}
  {%- endif -%}
{%- endif -%}

{%- comment -%} Find Essentials Product (Bands) {%- endcomment -%}
{%- assign essentials_keyword = 'bands' -%}
{%- assign essentials_product = blank -%}
{%- for p in collections.all.products -%}
  {%- assign title_down = p.title | downcase -%}
  {%- if title_down contains 'bands' or title_down contains 'bandes' or title_down contains 'resistance' -%}
    {%- assign essentials_product = p -%}
    {%- break -%}
  {%- endif -%}
{%- endfor -%}
{%- assign pro_product = product -%}  --&gt;


<script src="{{ 'strongside-product.js' | asset_url }}" defer="defer"></script>

<style>
  .strongside-hero {
    min-height: 100vh;
    background-color: #050505; /* brand-black */
    color: white;
    font-family: var(--font-body-family);
  }

  .strongside-hero-container {
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 1024px) {
    .strongside-hero-container {
      flex-direction: row;
    }
  }

  /* LEFT COLUMN - GALLERY */
  .strongside-gallery-col {
    width: 100%;
    background-color: #171717; /* neutral-900 */
    position: relative;
  }

  @media (min-width: 1024px) {
    .strongside-gallery-col {
      width: 55%;
    }
  }

  .strongside-gallery-sticky {
    padding: 1rem;
  }

  @media (min-width: 1024px) {
    .strongside-gallery-sticky {
      position: sticky;
      top: 4rem; /* top-16 */
      padding: 1.5rem;
    }
  }

  .strongside-gallery-grid {
    display: flex;
    gap: 1rem;
  }

  /* Thumbnails */
  .strongside-thumbs-desktop {
    display: none;
    flex-direction: column;
    gap: 0.5rem;
    max-height: 70vh;
    overflow-y: auto;
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .strongside-thumbs-desktop::-webkit-scrollbar {
    display: none;
  }

  @media (min-width: 1024px) {
    .strongside-thumbs-desktop {
      display: flex;
    }
  }

  .strongside-thumb-btn {
    width: 4rem; /* w-16 */
    height: 4rem; /* h-16 */
    border-radius: 0.5rem;
    overflow: hidden;
    border: 2px solid rgba(255, 255, 255, 0.1);
    opacity: 0.7;
    transition: all 0.2s;
    cursor: pointer;
    flex-shrink: 0;
  }

  .strongside-thumb-btn:hover {
    border-color: rgba(255, 255, 255, 0.3);
    opacity: 1;
  }

  .strongside-thumb-btn.is-active {
    border-color: #ff2222; /* brand-accent */
    box-shadow: 0 0 0 2px rgba(255, 34, 34, 0.3);
    transform: scale(1.05);
    opacity: 1;
  }

  /* Main Image */
  .strongside-main-image-container {
    flex: 1;
    aspect-ratio: 1/1;
    position: relative;
    overflow: hidden;
    border-radius: 1rem;
    background-color: #262626; /* neutral-800 */
    cursor: zoom-in;
  }

  .strongside-main-image {
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: opacity 0.3s;
  }

  /* Zoom Hint */
  .strongside-zoom-hint {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: rgba(0, 0, 0, 0);
    transition: background-color 0.3s;
    pointer-events: none;
  }

  .strongside-main-image-container:hover .strongside-zoom-hint {
    background-color: rgba(0, 0, 0, 0.2);
  }

  .strongside-zoom-label {
    opacity: 0;
    transition: opacity 0.3s;
    background-color: rgba(0, 0, 0, 0.5);
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 0.875rem;
  }

  .strongside-main-image-container:hover .strongside-zoom-label {
    opacity: 1;
  }

  /* Mobile Thumbs */
  .strongside-thumbs-mobile {
    display: flex;
    gap: 0.5rem;
    overflow-x: auto;
    padding-bottom: 0.5rem;
    margin-top: 0.5rem;
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  @media (min-width: 1024px) {
    .strongside-thumbs-mobile {
      display: none;
    }
  }

  /* RIGHT COLUMN - INFO */
  .strongside-info-col {
    width: 100%;
    background-color: #050505;
    padding: 1rem;
    display: flex;
    flex-direction: column;
  }

  @media (min-width: 1024px) {
    .strongside-info-col {
      width: 45%;
      padding: 4rem;
    }
  }

  .strongside-info-sticky {
    padding-top: 1rem;
  }

  @media (min-width: 1024px) {
    .strongside-info-sticky {
      position: sticky;
      top: 5rem;
    }
  }

  /* Product Title */
  .strongside-product-title {
    font-size: 2.25rem; /* text-4xl */
    font-weight: 900;
    text-transform: uppercase;
    font-style: italic;
    letter-spacing: -0.05em;
    line-height: 0.9;
    margin-bottom: 1.5rem;
    margin-top: 0.5rem;
  }

  @media (min-width: 1024px) {
    .strongside-product-title {
      font-size: 3.75rem; /* text-6xl */
      margin-top: 0;
    }
  }

  /* Description Toggle */
  .strongside-desc-toggle {
    width: 100%;
    margin-bottom: 1.5rem;
    padding: 0.75rem 1rem;
    background-color: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    cursor: pointer;
    transition: all 0.2s;
    color: white;
  }

  .strongside-desc-toggle:hover {
    background-color: rgba(255, 255, 255, 0.15);
  }

  .strongside-desc-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease-out;
    margin-bottom: 1.5rem;
  }

  .strongside-desc-content.is-open {
    max-height: 500px; /* arbitrary large value */
  }

  /* Price */
  .strongside-price-container {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .strongside-price-main {
    font-size: 2.25rem; /* text-4xl */
    font-weight: 900;
    color: white;
  }

  .strongside-price-currency {
    font-size: 1.25rem;
    font-weight: 400;
    color: #9ca3af;
  }

  .strongside-price-compare {
    font-size: 1.5rem; /* text-2xl */
    color: #9ca3af;
    text-decoration: line-through;
  }

  .strongside-discount-badge {
    padding: 0.25rem 0.5rem;
    background-color: #ff2222;
    color: white;
    font-size: 0.875rem;
    font-weight: 700;
    border-radius: 9999px;
  }

  /* Reviews Badge */
  .strongside-reviews-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.5rem 1rem;
    background-color: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 9999px;
    margin-bottom: 1.5rem;
    cursor: pointer;
    transition: all 0.2s;
    color: white;
  }

  .strongside-reviews-badge:hover {
    background-color: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.2);
  }

  /* Variants */
  .strongside-variants {
    margin-bottom: 2rem;
  }

  .strongside-variant-label {
    font-size: 0.75rem;
    font-weight: 700;
    color: #6b7280;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 0.75rem;
    display: block;
  }

  .strongside-variant-btn {
    padding: 0.75rem 1.5rem;
    border-radius: 9999px;
    font-size: 0.875rem;
    font-weight: 700;
    text-transform: uppercase;
    border: 1px solid rgba(255, 255, 255, 0.2);
    background: transparent;
    color: #9ca3af;
    cursor: pointer;
    margin-right: 0.5rem;
  }

  .strongside-variant-btn.is-selected {
    background: white;
    color: black;
    border-color: white;
  }

  /* Action Buttons */
  .strongside-actions {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 2rem;
  }

  .strongside-btn-add {
    width: 100%;
    padding: 0.75rem;
    background-color: white;
    color: black;
    font-size: 1.125rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: 9999px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    cursor: pointer;
    transition: all 0.2s;
    border: 2px solid rgba(255, 255, 255, 0.2);
  }

  .strongside-btn-add:hover {
    background-color: #f3f4f6;
  }

  .strongside-btn-buy {
    width: 100%;
    padding: 0.75rem;
    background-color: #10b981; /* emerald-500 */
    color: white;
    font-size: 1rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: 9999px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    cursor: pointer;
    transition: all 0.2s;
    box-shadow: 0 10px 15px -3px rgba(16, 185, 129, 0.3);
    border: none;
  }

  .strongside-btn-buy:hover {
    background-color: #059669; /* emerald-600 */
  }

  @media (min-width: 1024px) {
    .strongside-btn-add { padding: 1.25rem; font-size: 1.25rem; }
    .strongside-btn-buy { padding: 1rem; font-size: 1.125rem; }
  }


  /* Trust Badges - Text */
  .strongside-trust-text {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    color: #6b7280;
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-top: 0.5rem;
  }

  @media (min-width: 1024px) {
    .strongside-trust-text { font-size: 0.75rem; }
  }
  
  .strongside-trust-item {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  /* Trust Badges - Logos */
  .strongside-trust-logos {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin-top: 0.5rem;
  }
  
  .strongside-trust-logo-img {
    height: 2.5rem; /* h-10 */
    object-fit: contain;
    opacity: 0.8;
  }
  
  @media (min-width: 1024px) {
    .strongside-trust-logo-img { height: 3.5rem; /* h-14 */ }
  }


  /* Accordion */
  .strongside-accordion {
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    margin-top: 1rem;
  }

  .strongside-accordion-btn {
    width: 100%;
    padding: 1rem 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: white;
    font-weight: 700;
    text-transform: uppercase;
    font-size: 0.875rem;
    cursor: pointer;
    background: none;
    border: none;
  }

  .strongside-accordion-content {
     max-height: 0;
     overflow: hidden;
     transition: max-height 0.3s;
     color: #d1d5db; /* gray-300 */
     font-size: 0.875rem;
     line-height: 1.6;
  }
  
  .strongside-accordion-content.is-open {
    max-height: 200px;
    padding-bottom: 1rem;
  }
</style>

<div class="strongside-hero">
  <div class="strongside-hero-container">
    
    <!-- LEFT COLUMN -->
    <div class="strongside-gallery-col">
      <div class="strongside-gallery-sticky">
        <div class="strongside-gallery-grid">
          
          <!-- Desktop Thumbs -->
          <div class="strongside-thumbs-desktop">
            {% for media in product.media %}
              <button class="strongside-thumb-btn {% if forloop.first %}is-active{% endif %}" onclick="changeHeroImage(this, '{{ media | image_url: width: 1200 }}')">
                <img src="{{ media | image_url: width: 200 }}" class="w-full h-full object-cover">
              </button>
            {% endfor %}
          </div>

          <!-- Main Image -->
          <div class="strongside-main-image-container" id="hero-main-image-container">
            <img id="hero-main-image" src="{{ product.featured_media | image_url: width: 1200 }}" class="strongside-main-image">
            <div class="strongside-zoom-hint">
              <span class="strongside-zoom-label">🔍 Cliquer pour agrandir</span>
            </div>
          </div>

        </div>

        <!-- Mobile Thumbs -->
        <div class="strongside-thumbs-mobile">
           {% for media in product.media %}
              <button class="strongside-thumb-btn {% if forloop.first %}is-active{% endif %}" style="width:3rem;height:3rem;" onclick="changeHeroImage(this, '{{ media | image_url: width: 1200 }}')">
                <img src="{{ media | image_url: width: 200 }}" class="w-full h-full object-cover">
              </button>
            {% endfor %}
        </div>
      </div>
    </div>

    <!-- RIGHT COLUMN -->
    <div class="strongside-info-col">
      <div class="strongside-info-sticky">
        
        <!-- REMOVED: Pro/Essentials Tabs -->

        <!-- Title -->
        <h1 class="strongside-product-title">{{ product.title }}</h1>

        <!-- Description Toggle -->
        <button class="strongside-desc-toggle" onclick="toggleDescription()" style="display:flex;align-items:center;justify-content:space-between;width:100%;padding:1rem;background:rgba(255,255,255,0.1);border:1px solid rgba(255,255,255,0.2);border-radius:0.5rem;cursor:pointer;">
          <span style="font-size:0.75rem;font-weight:700;text-transform:uppercase;color:white;">Voir la description</span>
           <div id="desc-arrow" style="width:20px;height:20px;transition:transform 0.3s;display:flex;align-items:center;">
             <svg width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" style="width:100%;height:100%;color:#9ca3af;">
               <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
             </svg>
           </div>
        </button>
        
        <div id="desc-content" class="strongside-desc-content" style="overflow:hidden;max-height:0;transition:max-height 0.3s ease-out;margin-bottom:1.5rem;">
          <div class="prose prose-invert text-gray-300 text-sm" style="padding-top:1rem;color:#d1d5db;line-height:1.6;">
             {% if product.description != blank %}
               {{ product.description }}
             {% else %}
               <p>Le harnais de boxe STRONGSIDE Pro est conçu pour maximiser la puissance et l'explosivité de vos mouvements. Testé par des athlètes professionnels, il offre une résistance progressive et une liberté de mouvement totale.</p>
               <ul style="list-style:disc;margin-left:1.5rem;margin-top:0.5rem;">
                 <li>Résistance ajustable</li>
                 <li>Compatible sac de frappe & sparring</li>
                 <li>Mousquetons renforcés</li>
               </ul>
             {% endif %}
          </div>
        </div>

        <!-- Price -->
        <div class="strongside-price-container">
           <span class="strongside-price-main">{{ product.price | money_without_trailing_zeros }}</span>
           {% if product.compare_at_price > product.price %}
             <span class="strongside-price-compare">{{ product.compare_at_price | money_without_trailing_zeros }}</span>
             <span class="strongside-discount-badge">-{{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}%</span>
           {% endif %}
        </div>

        <!-- Reviews Badge -->
        <button class="strongside-reviews-badge">
           <div style="color:#eab308;display:flex;gap:2px;">
             <span>★</span><span>★</span><span>★</span><span>★</span><span>★</span>
           </div>
           <span style="font-size:0.75rem;color:#9ca3af;margin-left:0.5rem;">Lire les commentaires</span>
        </button>

        <!-- Variants -->
        {% unless product.has_only_default_variant %}
          <div class="strongside-variants">
            <label class="strongside-variant-label">Choisir une option</label>
            <div class="flex flex-wrap gap-2">
              {% for variant in product.variants %}
                <button 
                  class="strongside-variant-btn {% if forloop.first %}is-selected{% endif %}" 
                  data-variant-id="{{ variant.id }}"
                  onclick="selectVariant(this, '{{ variant.id }}', '{{ variant.price | money }}')"
                  style="border:1px solid {% if forloop.first %}white{% else %}rgba(255,255,255,0.2){% endif %}; background:{% if forloop.first %}white{% else %}transparent{% endif %}; color:{% if forloop.first %}black{% else %}#9ca3af{% endif %};"
                >
                  {{ variant.title }}
                </button>
              {% endfor %}
            </div>
          </div>
        {% endunless %}

        <!-- Actions -->
        {% if product %}
        {% form 'product', product %}
          <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
          
          <div class="strongside-actions">
            <!-- ADD TO CART: GREEN BUTTON, WHITE TEXT -->
            <button type="submit" name="add" class="strongside-btn-add" style="background-color:#10b981 !important; color:white !important; border:none !important; width:100%; border-radius:9999px; font-weight:900; text-transform:uppercase; padding:1.25rem;">
               <svg style="width:24px;height:24px;" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" /></svg>
               Ajouter au Panier
            </button>
            
            <!-- BUY NOW: WHITE BUTTON, BLACK TEXT -->
            <button type="submit" name="add" class="strongside-btn-buy" style="background-color:white !important; color:black !important; border:none !important; width:100%; border-radius:9999px; font-weight:900; text-transform:uppercase; padding:1rem; box-shadow: 0 10px 15px -3px rgba(255, 255, 255, 0.1);">
               <svg style="width:20px;height:20px;" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
               Achat Immédiat
            </button>
          </div>
        {% endform %}
        {% else %}
          <div style="padding: 2rem; border: 1px dashed red; color: red;">
            PRODUIT NON TROUVÉ
          </div>
        {% endif %}

        <!-- Trust Badges -->
        <div class="flex flex-col items-center justify-center gap-2 mt-2">
			<div class="strongside-trust-text">
				<span class="strongside-trust-item">
					<svg style="width:16px;height:16px;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" /></svg>
					Livraison Rapide
				</span>
				<span class="strongside-trust-item">
					<svg style="width:16px;height:16px;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" /></svg>
					Garantie
				</span>
				<span class="strongside-trust-item">
					<svg style="width:16px;height:16px;" fill="currentColor" viewBox="0 0 24 24"><path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z" /></svg>
					Paiement sécurisé
				</span>
			</div>
            <div class="strongside-trust-logos">
               <img src="{{ 'visa mastercard.webp' | asset_url }}" alt="Visa Mastercard" class="strongside-trust-logo-img">
               <img src="{{ 'paypal logo.webp' | asset_url }}" alt="PayPal" class="strongside-trust-logo-img">
               <img src="{{ 'cb logo.webp' | asset_url }}" alt="CB" class="strongside-trust-logo-img">
            </div>
        </div>

        <!-- Accordions -->
        <div class="border-t border-white/10 pt-4 mt-6">
           {% for block in section.blocks %}
             {% if block.type == 'info_row' %}
               <div class="strongside-accordion">
                 <button class="strongside-accordion-btn" onclick="toggleAccordion(this)">
                   {{ block.settings.title }}
                   <span class="plus-icon">+</span>
                 </button>
                 <div class="strongside-accordion-content">
                   {{ block.settings.content }}
                 </div>
               </div>
             {% endif %}
           {% endfor %}
        </div>

      </div>
    </div>
  </div>
</div>

<script>
  function changeHeroImage(btn, src) {
     document.getElementById('hero-main-image').src = src;
     document.querySelectorAll('.strongside-thumb-btn').forEach(b => b.classList.remove('is-active'));
     btn.classList.add('is-active');
  }

  function toggleDescription() {
     const desc = document.getElementById('desc-content');
     const arrow = document.getElementById('desc-arrow');
     desc.classList.toggle('is-open');
     arrow.style.transform = desc.classList.contains('is-open') ? 'rotate(180deg)' : 'rotate(0deg)';
  }

  function selectVariant(btn, id, price) {
     document.querySelectorAll('.strongside-variant-btn').forEach(b => b.classList.remove('is-selected'));
     btn.classList.add('is-selected');
     document.querySelector('input[name="id"]').value = id;
     // Update price display if needed (would need to parse money)
  }

  function toggleAccordion(btn) {
     const content = btn.nextElementSibling;
     const icon = btn.querySelector('.plus-icon');
     content.classList.toggle('is-open');
     icon.textContent = content.classList.contains('is-open') ? '-' : '+';
  }
</script>

{% schema %}
{
  "name": "Strongside Hero",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Product"
    }
  ],
  "blocks": [
    {
      "type": "info_row",
      "name": "Info Row",
      "settings": [
        {
           "type": "text",
           "id": "title",
           "label": "Title",
           "default": "Shipping & Returns"
        },
        {
           "type": "richtext",
           "id": "content",
           "label": "Content",
           "default": "<p>Content here</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Strongside Hero",
      "blocks": [
        {
          "type": "info_row",
          "settings": {
            "title": "Contenu du Pack",
            "content": "<p>Bandes de résistance, Guide, Sac...</p>"
          }
        },
        {
          "type": "info_row",
          "settings": {
            "title": "Livraison & Retours",
            "content": "<p>Expédition 24h...</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
